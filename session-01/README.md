--------- session-01 ---------

setup:
- install Ubuntu and run `uname -a`

research:
- compare user mode vs kernel mode (what happens if each one crashes?)

coding:
- print environment variables using `os.environ`
